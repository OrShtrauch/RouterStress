package main

//"fmt"
//"RouterStress/docker"
//"RouterStress/dhcp"

const (
	IP   = "192.168.0.1"
	PORT = "2323"
)

func main() {

	// fmt.Println(ip)
	// fmt.Println(len(server.Used))

	// fmt.Printf("\n\n\n")
	// server.Release(ip)
	// fmt.Println(len(server.Used))
	// var wg sync.WaitGroup
	// for i := 1; i < 5; i++ {
	// 	wg.Add(1)

	// 	i := i
	// 	go func() {
	// 		defer wg.Done()
	// 		worker(i)
	// 	}()

	// }

	// wg.Wait()

	// fmt.Printf("done\n")

	// x := 5

	// y := x + 5

	// fmt.Println(y)
}
